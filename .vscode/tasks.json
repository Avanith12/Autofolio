
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "server",
      "type": "shell",
      "command": "npm start",
      "options": { "cwd": "${workspaceFolder}/app/server" }
    },
    {
      "label": "client",
      "type": "shell",
      "command": "npm run dev",
      "options": { "cwd": "${workspaceFolder}/app/client" }
    }
  ]
}
